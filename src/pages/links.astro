---
import { getEntry, render } from "astro:content";
import Markdown from "@components/misc/Markdown.astro";
import { siteConfig } from "../config";
import { getShuffledLinksList } from "../data/links";
import I18nKey from "../i18n/i18nKey";
import { i18n } from "../i18n/translation";
import MainGridLayout from "../layouts/MainGridLayout.astro";

// 檢查連結頁面是否啟用
// if (!siteConfig.featurePages.links) {
//   return Astro.redirect("/404/");
//   }

const linksPost = await getEntry("spec", "links");

if (!linksPost) {
	throw new Error("links page content not found");
}

const { Content } = await render(linksPost);
const linksList = getShuffledLinksList();

// 獲取所有標籤
const allTags = Array.from(new Set(linksList.flatMap((item) => item.tags)));
---

<MainGridLayout title={i18n(I18nKey.usefulLinks)} description={i18n(I18nKey.usefulLinks)}>
	<!-- 只在連結頁面載入腳本 -->
	<script is:inline>
		// 標記當前頁面為連結頁面
		window.isLinksPage = true;
	</script>
	<script>
		import('../scripts/right-sidebar-layout.js');
	</script>
	<div class="flex w-full rounded-(--radius-large) overflow-hidden relative min-h-32">
		<div class="card-base z-10 px-9 py-6 relative w-full">
			<!-- 頁面標題 -->
			<div class="flex flex-col items-start justify-center mb-8">
				<h1 class="text-4xl font-bold text-90 mb-2 relative
							before:w-1 before:h-8 before:rounded-md before:bg-(--primary)
							before:absolute before:top-1/2 before:-translate-y-1/2 before:-left-4">
					{i18n(I18nKey.usefulLinks)}
				</h1>
				<p class="text-lg text-60">
					{i18n(I18nKey.usefulLinksSubtitle)}
				</p>
			</div>

			<!-- 搜尋和篩選欄 -->
			<div class="mb-6 space-y-3">
				<!-- 搜尋框 -->
				<div class="w-full">
					<div class="relative">
						<input 
							type="text" 
							id="link-search"
							placeholder={i18n(I18nKey.usefulLinksSearchPlaceholder)}
							class="w-full px-4 py-2 pl-10 rounded-lg bg-(--btn-regular-bg) 
									text-90
									border border-(--line-divider)
									focus:outline-none focus:ring-2 focus:ring-(--primary)/50
									transition-all duration-200"
						/>
						<svg class="absolute left-3 top-1/2 -translate-y-1/2 w-5 h-5 text-50" 
							 fill="none" stroke="currentColor" viewBox="0 0 24 24">
							<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
								d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
						</svg>
					</div>
				</div>

				<!-- 標籤篩選 -->
				<div class="filter-container flex flex-wrap gap-2">
					<button 
						class="filter-tag active"
						data-tag="all"
					>
						{i18n(I18nKey.usefulLinksFilterAll)}
					</button>
					{allTags.map(tag => (
						<button 
							class="filter-tag"
							data-tag={tag}
						>
							{tag}
						</button>
					))}
				</div>
			</div>

			<!-- 連結卡片網格 -->
			<div id="links-grid" class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-6 mb-6">
				{linksList.map((item) => (
					<div 
						class="link-card group relative bg-transparent rounded-xl border border-(--line-divider)
								 overflow-hidden transition-all duration-300
								 hover:shadow-xl hover:-translate-y-1"
						data-title={item.title.toLowerCase()}
						data-desc={item.desc.toLowerCase()}
						data-tags={item.tags.join(',')}
					>
						<!-- 卡片內容 -->
						<div class="p-6">
							<!-- 圖示和標題區 -->
							<div class="flex items-start gap-4 mb-4">
								<!-- 網站圖示 -->
								<div class="w-16 h-16 shrink-0 rounded-xl overflow-hidden 
												bg-(--btn-regular-bg) 
												ring-2 ring-transparent
												transition-all duration-300">
									<img 
										src={item.imgurl} 
										alt={item.title}
										class="w-full h-full object-cover transform group-hover:scale-110 transition-transform duration-300"
										loading="lazy"
									/>
								</div>

								<!-- 標題和連結 -->
								<div class="flex-1 min-w-0">
									<h3 class="text-xl font-bold text-90 mb-1 truncate
												 group-hover:text-(--primary) transition-colors duration-200">
										{item.title}
									</h3>
									<a 
										href={item.siteurl} 
										target="_blank" 
										rel="noopener noreferrer"
										class="text-xs text-50 hover:text-(--primary) 
												 truncate block transition-colors duration-200"
									>
										{new URL(item.siteurl).hostname}
									</a>
								</div>
							</div>

							<!-- 描述 -->
							<p class="text-sm text-60 mb-4 line-clamp-2 min-h-10">
								{item.desc}
							</p>

							<!-- 標籤 -->
							<div class="flex flex-wrap gap-2 mb-4">
								{item.tags.map(tag => (
									<span class="px-2 py-1 text-xs rounded-md 
													bg-(--primary)/10 text-(--primary) 
													font-medium">
										{tag}
									</span>
								))}
							</div>

							<!-- 操作按鈕 -->
							<div class="flex gap-2">
								<a 
									href={item.siteurl}
									target="_blank"
									rel="noopener noreferrer"
									class="flex-1 flex items-center justify-center gap-2 px-4 py-2 
											 rounded-lg bg-(--primary) text-white 
											 hover:bg-(--primary)/90 
											 active:scale-95 transition-all duration-200
											 font-medium text-sm"
								>
									<svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
										<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
											d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
									</svg>
									{i18n(I18nKey.usefulLinksVisit)}
								</a>
								<button 
									class="copy-link-btn px-3 py-2 rounded-lg 
											 bg-(--btn-regular-bg) 
											 hover:bg-(--btn-regular-bg-hover) 
											 active:scale-95 transition-all duration-200
											 text-70"
									data-url={item.siteurl}
									title={i18n(I18nKey.usefulLinksCopyLink)}
								>
									<svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
										<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
											d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z"></path>
									</svg>
								</button>
							</div>
						</div>

						<!-- 懸停裝飾效果 -->
						<div class="absolute inset-0 bg-linear-to-br from-(--primary)/5 to-transparent 
										opacity-0 group-hover:opacity-100 transition-opacity duration-300 pointer-events-none">
						</div>
					</div>
				))}
			</div>

			<!-- 無結果提示 -->
			<div id="no-results" class="hidden text-center py-12">
				<svg class="w-16 h-16 mx-auto mb-4 text-black/20 dark:text-white/20" fill="none" stroke="currentColor" viewBox="0 0 24 24">
					<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
						d="M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
				</svg>
				<p class="text-black/50 dark:text-white/50 text-lg">{i18n(I18nKey.usefulLinksNoResults)}</p>
			</div>

			<!-- 說明文件 -->
			<Markdown class="mt-8 prose dark:prose-invert max-w-none">
				<Content />
			</Markdown>
			
			<!-- 隱藏元素用於傳遞 i18n 文字到全域腳本 -->
			<div id="links-copy-success-text" style="display: none;">{i18n(I18nKey.usefulLinksCopySuccess)}</div>
		</div>
	</div>
	
	<!-- 引用全域腳本 -->
	<script is:inline src="/js/links-page-handler.js"></script>
</MainGridLayout>

<style>
	/* 連結卡片動畫 */
	.link-card {
		animation: fadeInUp 0.5s ease-out forwards;
		opacity: 0;
	}

	.link-card:nth-child(1) { animation-delay: 0.05s; }
	.link-card:nth-child(2) { animation-delay: 0.1s; }
	.link-card:nth-child(3) { animation-delay: 0.15s; }
	.link-card:nth-child(4) { animation-delay: 0.2s; }
	.link-card:nth-child(5) { animation-delay: 0.25s; }
	.link-card:nth-child(6) { animation-delay: 0.3s; }

	@keyframes fadeInUp {
		from {
			opacity: 0;
			transform: translateY(20px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	/* 搜尋框焦點效果 */
	#link-search:focus {
		box-shadow: 0 0 0 3px var(--primary) / 0.1;
	}
	
	/* 標籤篩選按鈕樣式 */
	.filter-container {
		display: flex;
		flex-wrap: wrap;
		gap: 0.5rem;
	}
	
	.filter-tag {
		padding: 0.5rem 1rem;
		border: 1px solid var(--line-divider);
		border-radius: var(--radius-large);
		background: var(--btn-regular-bg);
		color: var(--btn-content);
		font-size: 0.875rem;
		font-weight: 500;
		cursor: pointer;
		transition: all 0.2s ease;
		white-space: nowrap;
	}
	
	.filter-tag:hover:not(.active) {
		background: var(--btn-hover-bg);
		border-color: var(--primary);
		transform: translateY(-1px);
	}
	
	.filter-tag.active {
		background: var(--primary);
		color: white;
		border-color: var(--primary);
	}
	
	.filter-tag.active:hover {
		background: var(--primary) !important;
		color: white !important;
		border-color: var(--primary) !important;
		transform: translateY(-1px);
	}
</style>
